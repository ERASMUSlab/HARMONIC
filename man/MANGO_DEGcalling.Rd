% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MANGO_DEGcalling.R
\name{MANGO_DEGcalling}
\alias{MANGO_DEGcalling}
\title{Call DEGs with DESeq2 and write per-contrast DEG files for MANGO}
\usage{
MANGO_DEGcalling(
  filepath,
  type = "standard",
  full_condition,
  number_of_rep,
  mango_design
)
}
\arguments{
\item{filepath}{Directory path containing input files (expects \code{count/RAWcount.txt}).}

\item{type}{DEG calling mode. One of \code{"standard"} or \code{"broad"}.
\code{"standard"} uses \code{adjp=0.05} and \code{FC=2}; \code{"broad"} uses
\code{adjp=0.05} and \code{FC=1.5}.}

\item{full_condition}{Character vector of condition names (must match sample condition labels).}

\item{number_of_rep}{Integer vector of replicate counts for each condition in \code{full_condition}.}

\item{mango_design}{Character vector specifying contrasts and direction to export.
Each element should be formatted like \code{"<objective>_<control>_<trend>"},
where \code{trend} is one of \code{"UP"}, \code{"DOWN"}, \code{"SIG"}.}
}
\value{
Invisibly returns \code{NULL}. Results are written to files under \code{filepath}.
}
\description{
Runs DESeq2 for multiple contrasts defined by \code{mango_design}, writes
normalized counts and DEG result tables, and appends selected DEG file paths
into \code{input_DEG_list.txt} under \code{filepath}.
}
